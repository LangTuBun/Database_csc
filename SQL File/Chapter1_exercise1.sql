#liệt kê tất cả nhân viên với các cột
SELECT * FROM employees; 

#Liệt kê tất csar nhân viên chỉ với cá cột id
SELECT id, full_name, salary FROM employees;

#liệt kê nhân viên có id là 1
select * from employees where id=1;

#liệt kê nhân viên có gender là 1  
select * from employees where Gender=1;

#liệt kê nhân viên có gender là 0
select * from employees where gender=0;